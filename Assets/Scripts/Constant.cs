namespace DefaultNamespace
{
    public static class Constant
    {
        // Enemy state enumeration
        public enum EnemyState
        {
            Idle,
            Move,
            Attack,
            Die,
            ChaseLookOnly
        }
    }
}
